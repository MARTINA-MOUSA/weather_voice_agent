# مساعد الطقس الصوتي - Weather Voice Agent

مساعد صوتي ذكي للاستعلام عن الطقس باستخدام Gemini AI مع واجهة Streamlit

## المميزات

- 🎤 تفاعل صوتي مباشر باللغة العربية
- 🤖 استخدام Gemini AI للفهم الطبيعي
- 🌤️ معلومات الطقس الفورية من OpenWeatherMap
- 🔊 ردود صوتية واضحة
- 🖥️ واجهة Streamlit حديثة وسهلة الاستخدام
- 📊 عرض تفصيلي لبيانات الطقس
- 🏗️ هيكل مشروع احترافي ومنظم

## هيكل المشروع

```
weather_voice_agent/
├── AI/                      # خدمات الذكاء الاصطناعي
│   ├── __init__.py
│   ├── gemini_service.py    # خدمة Gemini AI
│   └── speech_service.py    # خدمة الصوت
├── backend/                 # الخلفية - الخدمات والمنطق
│   ├── __init__.py
│   ├── config.py            # إعدادات المشروع
│   ├── weather_service.py   # خدمة الطقس
│   └── utils/               # أدوات مساعدة
│       ├── __init__.py
│       └── helpers.py
├── frontend/                # الواجهة
│   └── app.py               # تطبيق Streamlit
├── main.py                  # تطبيق سطر الأوامر
├── requirements.txt         # المكتبات المطلوبة
├── env_template.txt         # قالب ملف المفاتيح
├── setup.py                 # سكريبت الإعداد
├── LICENSE                  # ترخيص المشروع
├── GITHUB_SETUP.md         # دليل إعداد GitHub
└── README.md               # هذا الملف
```

## المتطلبات

- Python 3.8 أو أحدث
- ميكروفون
- اتصال بالإنترنت

## التثبيت

### 1. تثبيت المكتبات المطلوبة

```bash
pip install -r requirements.txt
```

**ملاحظة:** على Windows، قد تحتاج لتثبيت PyAudio بشكل منفصل:

```bash
pip install pipwin
pipwin install pyaudio
```

أو قم بتحميل wheel file من [هنا](https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio)

### 2. إعداد المفاتيح

1. أنشئ ملف `.env` في مجلد المشروع:
```bash
# على Windows
copy env_template.txt .env

# على Linux/Mac
cp env_template.txt .env
```

2. احصل على مفتاح Gemini API:
   - اذهب إلى [Google AI Studio](https://makersuite.google.com/app/apikey)
   - أنشئ حساب أو سجل الدخول
   - أنشئ مفتاح API جديد
   - انسخه وضعه في `.env` بجانب `GEMINI_API_KEY=`

3. احصل على مفتاح Weather API:
   - اذهب إلى [OpenWeatherMap](https://openweathermap.org/api)
   - سجل حساب مجاني
   - احصل على API Key
   - انسخه وضعه في `.env` بجانب `WEATHER_API_KEY=`

### 3. تشغيل البرنامج

#### تشغيل واجهة Streamlit (موصى به):
```bash
streamlit run frontend/app.py
```

#### تشغيل تطبيق سطر الأوامر:
```bash
python main.py
```

## الاستخدام

1. شغّل البرنامج
2. في Streamlit: اضغط على "🚀 تهيئة التطبيق" أولاً
3. تحدث بالسؤال عن الطقس (مثال: "ما هو الطقس في القاهرة؟")
4. سيستمع البرنامج ويجيب صوتياً

## أمثلة على الأسئلة

- "ما هو الطقس في القاهرة؟"
- "كيف الطقس اليوم في دبي؟"
- "أخبرني عن الطقس في الرياض"
- "ما هي درجة الحرارة في لندن؟"

## استكشاف الأخطاء

### مشكلة في التعرف على الصوت
- تأكد من أن الميكروفون يعمل
- تحقق من صلاحيات الوصول للميكروفون
- جرب زيادة وقت الاستماع في الكود

### مشكلة في PyAudio
- على Windows: استخدم `pipwin` أو قم بتحميل wheel file
- على Linux: `sudo apt-get install portaudio19-dev python3-pyaudio`
- على Mac: `brew install portaudio`

### خطأ في API Keys
- تأكد من نسخ المفاتيح بشكل صحيح في ملف `.env`
- تأكد من عدم وجود مسافات إضافية
- تحقق من أن المفاتيح صالحة وغير منتهية الصلاحية

## ربط المشروع بـ GitHub

لربط المشروع بـ GitHub، اتبع التعليمات في ملف [GITHUB_SETUP.md](GITHUB_SETUP.md)

### أوامر سريعة:

```bash
# تهيئة Git
git init

# إضافة الملفات
git add .

# Commit
git commit -m "Initial commit: Weather Voice Agent"

# إضافة Remote (استبدل بالرابط الصحيح)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# رفع الملفات
git push -u origin main
```

⚠️ **مهم**: تأكد من أن ملف `.env` موجود في `.gitignore` ولا يتم رفعه!

## المساهمة

نرحب بجميع المساهمات! يرجى:
1. عمل Fork للمشروع
2. إنشاء فرع جديد (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push إلى الفرع (`git push origin feature/AmazingFeature`)
5. فتح Pull Request

## الترخيص

هذا المشروع مرخص تحت [MIT License](LICENSE) - راجع ملف LICENSE للتفاصيل.
