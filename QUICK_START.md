# دليل البدء السريع

## خطوات سريعة للتشغيل

### 1. تثبيت المكتبات
```bash
pip install -r requirements.txt
```

### 2. إنشاء ملف .env
```bash
# Windows
copy env_template.txt .env

# أو استخدم setup.py
python setup.py
```

### 3. إضافة المفاتيح
افتح ملف `.env` وأضف:
- `GEMINI_API_KEY`: من [Google AI Studio](https://makersuite.google.com/app/apikey)
- `WEATHER_API_KEY`: من [OpenWeatherMap](https://openweathermap.org/api)

### 4. التشغيل

#### واجهة Streamlit (موصى به):
```bash
streamlit run app.py
```

أو على Windows:
```bash
run_streamlit.bat
```

#### تطبيق سطر الأوامر:
```bash
python main.py
```

## نصائح سريعة

- تأكد من أن الميكروفون يعمل
- تحدث بوضوح وباللغة العربية
- أمثلة: "ما هو الطقس في القاهرة؟" أو "كيف الطقس في دبي؟"
- في واجهة Streamlit: اضغط على "🎤 تحدث" للبدء
- قل "إنهاء" للخروج (في تطبيق CLI)

## حل المشاكل السريع

**مشكلة PyAudio على Windows:**
```bash
pip install pipwin
pipwin install pyaudio
```

**مشكلة في المفاتيح:**
- تأكد من عدم وجود مسافات قبل أو بعد المفتاح
- تأكد من أن المفاتيح صالحة

**مشكلة في الميكروفون:**
- تحقق من إعدادات Windows للميكروفون
- جرب ميكروفون آخر

**مشكلة في الاستيرادات:**
- تأكد من أنك في المجلد الصحيح
- جرب: `python -m streamlit run app.py`
